[
    {
        "context": "Workspace",
        "use_key_equivalents": true,
        "bindings": {
            "ctrl-r": "projects::OpenRecent" // TODO(https://github.com/zed-industries/zed/issues/23886): Use `Workspace && !Terminal` context instead and remove `Terminal` context override
        }
    },
    {
        "context": "Terminal",
        "use_key_equivalents": true,
        "bindings": { "ctrl-r": ["terminal::SendKeystroke", "ctrl-r"] }
    }
]
