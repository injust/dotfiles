[alias]
	br = branch
	co = checkout
	cp = cherry-pick
	df = diff
	dfc = diffc
	diffc = diff --cached
	lol = log --decorate --oneline --graph
	lola = lol --all
	rb = rebase
	rba = rebase --abort
	rbc = rebase --continue
	rbi = rebase --interactive
	sh = show
	st = status
	sw = switch

[commit]
	gpgSign = true

[core]
	fsmonitor = true
	# Override `LESS=FRX` default to unbreak mouse scrolling (https://github.com/gwsw/less/issues/445)
	pager = LESS='--quit-if-one-screen --RAW-CONTROL-CHARS' delta
	untrackedcache = true

[credential]
	helper = 

[delta]
	navigate = true

[diff]
	colorMoved = default

[fetch]
	prune = true

[gpg]
	format = ssh

[init]
	defaultBranch = main

[interactive]
	diffFilter = delta --color-only

[merge]
	conflictstyle = diff3

[pull]
	rebase = true

[submodule]
	recurse = true

[user]
	email = injustsu@gmail.com
	name = Justin Su
	signingKey = ~/.ssh/keys/github-signing
